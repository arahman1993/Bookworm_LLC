A database management system (DBMS) that can be used by a small bookstore. The features are:
* Having a DBMS that is connected to a front facing web interface.
* The UI dashboard will be able to access, create and modify records within the database.
* Separate views for managers, employees and customers.
* Authorization even/login to access for each view.
* To manage sales, search, updating and checking inventory, generating audit reports.
